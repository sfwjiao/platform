(function () {
    var controllerId = "app.views.manage.testing.answerSheet";
    angular.module("app").controller(controllerId, [
        "$scope",
        "appSession",
        "$state",
        function (
            $scope,
            appSession,
            $state
            ) {

            var vm = this;
        }
    ]);
})();