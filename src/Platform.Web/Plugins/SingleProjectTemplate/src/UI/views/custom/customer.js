(function () {
    var controllerId = "singleProjectTemplate.views.manage.customer";
    angular.module("app").controller(controllerId, [
        "$scope",
        function (
            $scope
            ) {

            $scope.init = function () {

            }

            $scope.init();
        }
    ]);
})();