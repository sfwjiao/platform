(function () {
    var controllerId = "Cms.views.Cms.index";
    angular.module("app").controller(controllerId, [
        "$scope",
        function (
            $scope
            ) {

            $scope.init = function () {

            }

            $scope.init();
        }
    ]);
})();