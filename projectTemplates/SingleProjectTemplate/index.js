(function () {
    var controllerId = "$projectname$.views.$projectname$.index";
    angular.module("app").controller(controllerId, [
        "$scope",
        function (
            $scope
            ) {

            $scope.init = function () {

            }

            $scope.init();
        }
    ]);
})();